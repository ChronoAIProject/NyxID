{
  "name": "@nyxids/sdk-workspace",
  "private": true,
  "workspaces": [
    "oauth-core",
    "oauth-react",
    "demo-react"
  ],
  "scripts": {
    "build": "npm run -ws build",
    "test": "npm run -w @nyxids/oauth-core test",
    "clean": "npm run -ws clean",
    "prepublish:all": "npm run -ws prepublishOnly"
  }
}
