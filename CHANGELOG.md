# Changelog

## Unreleased

- Switched `nyxid` installs and self-updates to signed prebuilt release binaries, added backend release metadata at `/cli/latest`, and enabled profile-scoped periodic update checks in the CLI.
